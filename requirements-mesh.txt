# Optional HF Depth Pro + PyTorch3D build helpers. Deliberately no torch/torchvision pin.
# Install PyTorch3D separately against the server's verified CUDA/PyTorch pair.
transformers==4.57.6
safetensors>=0.4,<1
ninja>=1.11
fvcore>=0.1.5
iopath>=0.1.9
